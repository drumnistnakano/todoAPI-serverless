{
  "name": "todoapi",
  "version": "1.0.0",
  "devDependencies": {
    "serverless-python-requirements": "^5.1.1"
  },
  "scripts": {
    "postinstall": "pip3 install -r requirements.txt",
    "test": "pytest"
  }
}